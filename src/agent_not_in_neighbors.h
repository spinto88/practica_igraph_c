#ifndef AGENT_NOT_IN_NEIGHBORS_H
#define AGENT_NOT_IN_NEIGHBORS_H

#include <stdlib.h>
#include <igraph.h>

int agent_not_in_neighbors(igraph_t *, int, int);
int virtual_neighbour(igraph_t *, int, int);

#endif
